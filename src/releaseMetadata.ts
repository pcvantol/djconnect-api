/**
 * The release-artifact workflow replaces this source file immediately before
 * bundling. Keeping a safe development value makes local builds explicit
 * without pretending they are a qualified release artifact.
 */
export const RELEASE_VERSION = "3.3.0";
export const RELEASE_SOURCE_SHA = "development";
