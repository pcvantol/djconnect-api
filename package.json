{
  "name": "djconnect-api",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "provision:cloudflare": "scripts/provision_cloudflare.sh"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.18",
    "@types/node": "^26.0.0",
    "typescript": "^5.5.2",
    "vitest": "~4.1.9",
    "wrangler": "^4.103.0"
  }
}
