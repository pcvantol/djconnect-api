# DJConnect API development extension

`djconnect-api` owns the Cloudflare Worker, D1 and APNs trust/relay implementation.
DJConnect remains the product-authority for cross-component architecture and roadmap.

Use `README.md`, `DEVELOPMENT_ENVIRONMENT.md`, `API_CONTRACT.md`, `TECHNICAL_DESIGN_DECISIONS.md`, and the existing Worker test/release workflows for local implementation and validation. The repository's `.tde.yml` and Trusted Delivery workflow remain its delivery evidence boundary.
