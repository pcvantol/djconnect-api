name: Validate

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  software-assurance-governance:
    name: Software Assurance governance
    permissions:
      contents: read
    uses: pcvantol/djconnect/.github/workflows/software-assurance-governance.yml@c34b9ba83de6d341ff223f5b2225624ccdab17c9
    with:
      execution_profile: balanced
      owner: pcvantol/djconnect-api

  validate:
    name: Validate
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: 26
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Dependency and tool freshness report
        run: |
          node --version
          npm --version
          npx wrangler --version
          npm run deps:report

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Wrangler deploy dry-run
        run: npx wrangler deploy --dry-run

      - name: Validate D1 migrations
        run: npx wrangler d1 migrations apply djconnect_api --local

      - name: Test
        run: npm test -- --run

      - name: Local E2E contract smoke
        run: npm run test:e2e

      - name: Postman contract smoke test
        run: npm run postman:test

      - name: Secret pattern scan
        run: |
          set -euo pipefail
          matches="$(rg -n "BEGIN PRIVATE KEY|AuthKey|DJCONNECT_RELAY_SECRET=|APNS_PRIVATE_KEY=|Bearer [A-Za-z0-9]|ghp_|sk-|spotify_refresh|refresh_token|device_token" . || true)"
          if [ -n "$matches" ]; then
            echo "$matches"
            disallowed="$(printf '%s\n' "$matches" | grep -Ev 'src/apns\.ts|worker-configuration\.d\.ts|AGENTS\.md|CONTRIBUTING\.md|DEVELOPMENT_ENVIRONMENT\.md|\.github/workflows/ci-cd\.yml' || true)"
            if [ -n "$disallowed" ]; then
              echo "Disallowed secret-like pattern found." >&2
              exit 1
            fi
          fi

  deploy:
    name: Deploy
    needs: validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: 26
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply remote D1 migrations
        run: npx wrangler d1 migrations apply djconnect_api --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Deploy Worker
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Health smoke test
        run: curl -fsS https://api.djconnect.dev/health

  staging-smoke:
    name: Staging-safe smoke E2E
    needs:
      - validate
      - deploy
    if: always() && needs.validate.result == 'success' && (needs.deploy.result == 'success' || needs.deploy.result == 'skipped') && (github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      HAS_DJCONNECT_RELAY_SECRET: ${{ secrets.DJCONNECT_RELAY_SECRET != '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: 26
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Skip staging smoke without relay secret
        if: env.HAS_DJCONNECT_RELAY_SECRET != 'true'
        run: echo "Skipping staging-safe smoke E2E because DJCONNECT_RELAY_SECRET is not available."

      - name: Run staging-safe smoke E2E
        if: env.HAS_DJCONNECT_RELAY_SECRET == 'true'
        run: npm run smoke:e2e
        env:
          DJCONNECT_RELAY_SECRET_VALUE: ${{ secrets.DJCONNECT_RELAY_SECRET }}

  cleanup-workflow-runs:
    name: Cleanup old workflow runs
    needs:
      - validate
      - deploy
    if: always() && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: write
      contents: read

    steps:
      - name: Delete old completed workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          CURRENT_RUN_ID: ${{ github.run_id }}
          KEEP_RUNS: 2
        run: |
          set -euo pipefail

          current_workflow_id="$(gh api "repos/${REPOSITORY}/actions/runs/${CURRENT_RUN_ID}" --jq '.workflow_id')"
          workflow_ids="$(gh api "repos/${REPOSITORY}/actions/workflows" --paginate --jq '.workflows[].id')"

          for workflow_id in ${workflow_ids}; do
            keep_limit="${KEEP_RUNS}"
            if [ "${workflow_id}" = "${current_workflow_id}" ]; then
              keep_limit="$((KEEP_RUNS - 1))"
            fi

            mapfile -t run_ids < <(
              gh api "repos/${REPOSITORY}/actions/workflows/${workflow_id}/runs?status=completed&per_page=100" \
                --paginate \
                --jq '.workflow_runs | sort_by(.created_at) | reverse | .[].id'
            )

            if [ "${#run_ids[@]}" -le "${keep_limit}" ]; then
              continue
            fi

            for index in "${!run_ids[@]}"; do
              run_id="${run_ids[$index]}"
              if [ "${index}" -lt "${keep_limit}" ] || [ "${run_id}" = "${CURRENT_RUN_ID}" ]; then
                continue
              fi

              echo "Deleting completed workflow run ${run_id} for workflow ${workflow_id}"
              gh api \
                --method DELETE \
                "repos/${REPOSITORY}/actions/runs/${run_id}"
            done
          done
