# DJConnect API bootstrap

Start with `docs/ai-development/GENERATED_PROJECTION.md`, then read `docs/ai-development/DJCONNECT_API_DEVELOPMENT_EXTENSION.md`, `AGENTS.md`, and the task-relevant local API, Worker and validation documentation. The projection supplies generic development procedure; this repository owns only the API implementation boundary. DJConnect remains product authority.
